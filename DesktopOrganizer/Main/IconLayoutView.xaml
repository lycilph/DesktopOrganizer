<UserControl x:Class="DesktopOrganizer.Main.IconLayoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <GroupBox Header="Icon Layout" Margin="0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="LayoutName" Style="{StaticResource ContentTextBlockStyle}"/>
            <TextBlock Grid.Column="1" x:Name="Shortcut" HorizontalAlignment="Right" Style="{StaticResource ContentTextBlockStyle}"/>
        </Grid>
    </GroupBox>
</UserControl>
